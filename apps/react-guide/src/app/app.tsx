import React from 'react';
import './app.scss';
import { Route } from 'react-router-dom';

export const App = () => {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./app.scss file.
   */
  return (
    <div className="app">
      {/* <Route path="" exact component={ReactGuideLib} /> */}
    </div>
  );
};

export default App;
